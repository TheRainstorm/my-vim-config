[user]
	name = Fuyan Yuan
	email = 33221728+TheRainstorm@users.noreply.github.com
[alias]
        last = log -1
        co = checkout
        cm = commit
        rb = rebase
        br = branch
        rt = remote
        st = status
        lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit -15
  lg-graph = log --oneline --graph --decorate --all
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[http]
  #proxy = socks5://127.0.0.1:1089
[https]
  #proxy = socks5://127.0.0.1:1089
